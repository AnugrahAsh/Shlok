<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= bhajan.title %></title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;500;700&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      background-color: #e0f7fa;
      font-family: 'Inter', sans-serif;
    }

    .container {
      max-width: 1200px;
      margin: 40px auto;
      padding: 0 20px;
    }

    .top-section {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      align-items: flex-start;
      background: white;
      padding: 30px;
      border-radius: 20px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      gap: 40px;
    }

    .details {
      flex: 1 1 50%;
      min-width: 300px;
      color: #1f1f1f;
    }

    .details h1 {
      font-size: 36px;
      margin-bottom: 20px;
      font-weight: 600;
      border-left: 5px solid #333;
      padding-left: 15px;
    }

    .lyrics-box {
      font-size: 20px;
      line-height: 1.8;
      background: #ffffff;
      padding: 20px;
      border-radius: 12px;
      box-shadow: inset 0 0 6px rgba(0, 0, 0, 0.05);
      text-align: left;
      max-height: 500px;
      overflow-y: auto;
      scroll-behavior: smooth
      /* white-space: pre-wrap; */
    }

    .video {
      flex: 1 1 40%;
      min-width: 300px;
      max-width: 560px;
      height: 315px;
      overflow: hidden;
      border-radius: 12px;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease;
    }

    .video iframe {
      width: 100%;
      height: 100%;
      border: none;
    }

    .video:hover {
      transform: scale(1.015);
    }

    @media (max-width: 768px) {
      .top-section {
        flex-direction: column;
        gap: 20px;
      }

      .video {
        width: 100%;
        height: 250px;
      }
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar.ejs') %>

  <div class="container">
    <% if (bhajan) { %>
      <div class="top-section">
        <div class="details">
          <h1><%= bhajan.title %></h1>
          <div class="lyrics-box">
            <%- bhajan.lyrics.replace(/\n/g, "<br>") %>
          </div>
        </div>
        <div class="video">
          <iframe loading="lazy" src="<%= bhajan.youtube_video %>" allowfullscreen></iframe>
        </div>
      </div>
    <% } else { %>
      <h1>Bhajan not found</h1>
    <% } %>
  </div>
</body>
</html>
